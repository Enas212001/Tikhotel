import 'package:flutter/material.dart';

class RequestTypesPage extends StatelessWidget {
  const RequestTypesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: CustomScrollView(slivers: [],),);
  }
}